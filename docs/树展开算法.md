# 树展开算法

本文说明本项目"记号树"的展开语义。行为自原 NE 项目保留(虽经重写但行为不变),
对应实现位于 `src/core/expander.ts`(`expand_item` / `expand_tier_impl` / `expand_single` / `generate_fs`)。
本文是语义说明与实现注释的权威参照。

## 1. 树的列表形式

- 树中所有表达式在**先根遍历**下**严格递减**。先根遍历得到的序列称为该树的**列表形式**。
- 在代码中,children 数组即按该顺序存储(首子最大);`find_next` / `get_bound` 语义都基于"列表的下一项"。

## 2. 单节点展开(列表视角)

设要展开的节点为 A,其列表形式的下一项为 B(必有 A > B)。

1. 反复取 A 的基本列 `FS(A, i)`,直到某一项 `> B`;
2. 把该项作为一个新节点插入到 A 与 B 之间。

### 无法展开的情形
- **0**:不能展开;
- **后继 x+1**:可展开一次,得到 x(若 x 尚未出现在树中);此后 bound 会挡住再次展开,故只能一次;
- **病态 gap**(非标准数据:FS 的任何项都不大于 B):视同"无法展开"处理即可——上层链停住,`generate_fs` 的试展开守卫仅用于防止页面卡死,不必为它做特殊语义。

## 3. 树形插入约束

将列表视角插入的新节点放进树时,遵循:

- 尽量插入在**尽可能浅**的层级;
- 任何节点的子节点**不允许从中间新增**,只允许从子列表的**两端**插入。

## 4. 首次展开的位置(三种情形)

设 A 是 X 的子节点:

1. **A 是 X 的末子节点**:A 的首次展开不能插到 X 外层,只能追加为 X 的末子
   (= A 的兄弟)。此时 **A 的单层展开 = 展完 X 的兄弟展开**。
2. **A 不是 X 的末子节点,且 A 没有子节点**:A 的首次展开作为 A 的首子插入;
   **A 的单层展开 = 展完 A 子节点的兄弟展开**。
3. **A 不是 X 的末子节点,且 A 已经有子节点**:A 的首次展开展出 Aₙ(作为 A 的首子),
   **A 的单层展开 = 展出 Aₙ,连同 Aₙ 子节点的全部兄弟展开**。

> 代码对照:`expand_item` 以 `as_sibling = 父非根 && A 为父的末子` 决定首次方向;
> `expand_single(as_sibling)` 中 `true → append_sibling`(兄弟),`false → prepend_child`(首子)。

## 5. 兄弟展开(单层链)

"兄弟展开"指沿列表连续插入:每步对刚生成/推进到的节点再做一次展开,
把新项插到该节点与它的列表下一项之间,直到 `expand_single` 返回 undefined(链终止)。

- **单层展开(tier = 1)**:先展开一次,然后**一直进行兄弟展开直到不能展开为止**。
- tier = 0(i18n "单次展开")只做单节点的一次展开。
- tier ≥ 2(多层):铺完本层兄弟链后,对链上每个节点做 tier-1 展开(见下)。

## 6. 多层(tier)与实现结构

实现中 `expand_tier_impl(node, ctx, tier, as_sibling)`:

- **兄弟链**:携带**同一 tier** 的递归调用,每步对 `expand_single` 刚生成的节点继续展开,
  tier 不递减,直到 `expand_single` 返回 undefined —— 这就是"不断进行的兄弟展开";
- **链内方向延续**:`next_as_sibling = as_sibling || node.children.length === 1`
  —— 一旦进入兄弟展开则保持;若某步以子节点插入后 node 恰好只有一个子,链后续项转为兄弟展开;
- **多层追加**:`tier > 1` 时,对链结构追加 `tier - 1` 的更深层展开
  (优先取新节点末子,无末子则再展开自身);其意图为:铺完兄弟链后,
  对链上节点再做下一层的展开。

> 注意:第 4 节三种情形、第 6 节的"链方向延续"与"多层追加"是历史实现中被判定为
> 行为正确、不可随意简化的部分。若需调整,请以"差分对拍(改前/改后展开结果完全一致)"
> 作为回归依据,并同步更新本文。

## 7. 相关参数与守卫

- `max_find_fs`(settings 可调)经 `main.ts` watch 同步到 expander 的模块级变量,
  不再作为参数透传;语义是"单次展开内连续 reject 阈值",用于拦截病态卡死。
- `debug_verification`(记号可选字段):每生成一个新节点对其表达式校验,
  失败仅 `console.warn` 且节点照常创建;仅调试用,发布前建议删除。
